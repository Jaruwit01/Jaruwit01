#a,b,c = [int(x)for x in input().split()] #split การรับค่าบรรทัดเดียวแบบเว้นช่องว่างโดยใช้ spaecbar 
# x = int  เอา x ไปวนลูปใน ค่าที่เรารับมจากคีย์บอร์ด เพื่อแปลงค่าที่รับมาให้กลายเป็น int 

#print(a,b,c)

#a,b,c, = input().split()
#for x in a,b,c: #วนลูป ตัวแปร x เพื่อเก็บค่าในตัวแปร a b c แล้วนำค่าแปลง เป็น int โดยเก็บค่าไว้ใน ตัวแปร f
    # f=int(x) 
    
a,b,c = input().split()#split การรับค่าบรรทัดเดียวแบบเว้นช่องว่างโดยใช้ spaecbar 
d=int(a)
e=int(b)
f=int(c)
print(d,e,f)